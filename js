// while loop
while (number < 10) {
  console.log(number);
  number++;
}

// SET is an array with a guarantee of no duplicates

// React App
import request from 'superagent';

<NavLink exact activeClassName="active" from="/search" />;

// you can use
export default withRouter(class SomeClass {});
// when defining class

// SORTING
// returns in ascending order
numbersArray.sort((a, b) => a - b);
// returns in reverse order
numbersArray.sort((a, b) => b - a);
// this sorts strings
wordsArray.sort((a, b) => a.localeCompare(b));
//sorts objects
objectsArray.sort((a, b) => a.objectKey - b.objectKey);

// sorting goes in the cool zone under render( )
